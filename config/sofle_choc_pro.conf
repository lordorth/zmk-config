# Deep Sleep (30 minutes)
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=1800000

# No Display
CONFIG_ZMK_DISPLAY=n

# Ensure Bluetooth power is default (remove +8dBm if it was set elsewhere)
# CONFIG_BT_CTLR_TX_PWR_PLUS_8 is not set
