# Right half overrides: peripheral split, no USB HID
CONFIG_ZMK_SPLIT=y
CONFIG_ZMK_USB=n

# Left/central handles BLE advertising and HID output, so only the
# peripheral board needs to keep the split transport alive.
